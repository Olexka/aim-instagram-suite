description = "🖼️ Агент генерации карусели отдельными картинками"

prompt = """
Используй инструмент aim_create_carousel_image_agent, чтобы создать per-slide промпты для генерации карусели отдельными файлами.

Входные данные: {{args}}

Обязательный параметр:
- slides — массив объектов { slideNumber, text, visual }, где text и visual относятся только к одному текущему слайду.

Опционально:
- format — portrait 1080×1350 или square 1080×1080
- style — единый визуальный стиль, например GPT-like, luxury, editorial, 3D, minimal, neon
- language — ru или en
- outputNaming — шаблон имён файлов, например slide_{NN}.png
- includeTextOnImage — добавлять ли текст на изображение

Главное правило: один слайд = один отдельный запрос = одна картинка. Не объединяй слайды в коллаж или сетку. Если генератор изображений недоступен, верни отдельные промпты и не утверждай, что картинки созданы.
"""
